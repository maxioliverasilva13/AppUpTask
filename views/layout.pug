<!DOCTYPE html>
html(lang="en")
    head
       include partials/header
    body

        header.barra
            h1 UpTask - ADministrador Proyectos
            a(href="/cerrar-sesion") Cerrar Sesion

        main.contenedor
            aside.contenedor-proyectos
                .panel.crear-proyecto
                    a(href="/nuevoproyecto" class="boton btn-new-proyect") New Proyecto
                        span +
                .panel.lista-proyectos
                    h2 Proyectos
                    ul#proyectos.proyectos
                        if(proyect.length)
                            each proyecto in proyect
                                li
                                    a(href=`/proyectos/${proyecto.url}`)= proyecto.id + "-" + proyecto.nombre 


            block contenido
            //pre= vardump(proyect)
        
        script(src="/dist/bundle.js") 

